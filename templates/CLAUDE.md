<!--
CLAUDE.md TEMPLATE — copy to your repository root.

Deliberately near-empty. It exists so that Claude Code finds a file where it looks for one, and it
routes to AGENTS.md rather than duplicating it. Two files that both describe how to work in a
repository will disagree within a month, and nobody will know which one is current.

A test asserts this file stays shorter than AGENTS.md and does not restate the load sequence.
-->

# Claude

Read [`AGENTS.md`](AGENTS.md) first. It carries the load sequence, the stop rule, and the commands.

Nothing in this file overrides it.
