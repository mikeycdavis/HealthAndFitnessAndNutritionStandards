---
id: ST-12
type: story
title: Self-maintenance eligibility rests on a signature a fork cannot hold
parent: FE-13
status: COMPLETE
closed: 2026-08-16
opened: 2026-08-16
evidence:
  - https://github.com/mikeycdavis/HealthAndFitnessAndNutritionStandards/pull/5
  - https://github.com/mikeycdavis/HealthAndFitnessAndNutritionStandards/pull/6
  - https://github.com/mikeycdavis/HealthAndFitnessAndNutritionStandards/pull/7
  - 274ba0a
tags: [release-identity, integrity]
---

## What

Make canonical-pack origin depend on verifying an immutable release's signature against a trust
anchor supplied from **outside** the pack being evaluated, rather than on the tag resolving to a
recorded commit oid.

## Why

[ADR 0009](../../adr/0009-self-maintenance-is-a-distinct-outcome.md) closed the escape that
independent review found — an adopter can no longer acquire self-maintenance by copying the evaluator
into a directory they control — and named the residual it did not close:

> A fork of this repository possesses every byte the original does — the certified tag, the recorded
> oid, the lineage, and the file explaining them — so no check running inside the evaluator
> distinguishes a fork from the pack.

The residual is bounded rather than dangerous. What eligibility buys is a `SELF_MAINTENANCE` outcome:
not a compliance verdict, from a command an adopter's pipeline does not run, carrying no authority
over anybody's adoption of anything. A fork that satisfies the conditions has gone to considerable
trouble for a result that fails every gate it would want to pass.

FE-13's original acceptance criterion — no adopter can satisfy `packSelfMaintenance` structurally —
was retired by the owner on 2026-08-16 and replaced with a boundary that names this residual and
assigns it here. Both records carry the approved wording: ADR 0009, and [FE-13](FE-13.md)'s
acceptance criteria. This item is where that criterion goes to be met, and until it is met nothing may
report FE-13 as having established fork exclusivity.

A signature is the only thing that would actually close it, because it is the only fact about the
release that cannot be copied along with the bytes — **provided the key it is checked against cannot
be copied along with the bytes either.**

## The custody decision, and the constraint that came with it

The blocking question — who holds the key — was answered by the owner on 2026-08-16 and is recorded
in full in [ADR 0010](../../adr/0010-release-signing-custody-and-an-external-trust-anchor.md):
Michael Davis as the accountable human release owner; the private key human-controlled, never
committed, never generated by the pack, never available to ordinary CI; **the trust anchor supplied
externally to the pack being evaluated**; no configured key is `NOT ESTABLISHED` rather than skipped;
a wrong or untrusted signature fails closed; rotation is a governance event; agents may verify and
prepare but never hold the signing authority.

The externality constraint is the part that changes the implementation rather than the paperwork. A
`scripts/trusted-key.json` living inside this repository is replaced by a fork along with everything
else, after which the fork signs its own release, verifies successfully, and reports a cryptographic
proof of nothing. That design would pass every obvious test.

## Acceptance

- **The falsifier, committed failing first.** Copy the entire repository, replace its trust
  configuration with an attacker-controlled public key, create a correctly signed release using the
  attacker's matching private key, and prove verification still rejects it because the authoritative
  anchor came from outside the fork. The fork in this test is competent: it signs correctly and is
  internally consistent, and fails on the one fact that is not its to change. **An implementation
  that passes only because the fork did not think to replace the key file has not met this criterion.**
- A fork with the full lineage and no valid signature is refused canonical origin.
- Verification failure, wrong-key, and verification-unavailable are distinct outcomes, as
  contradicted and unavailable already are in `scripts/pack-lineage.mjs`. Unavailable must not pass,
  and must never be reported as skipped.
- `scripts/certified-releases.json` records per release whether it was signed under this regime and
  against which key identity, so a release predating it says so rather than being silently treated as
  unsigned-therefore-fine or unsigned-therefore-broken.
- ADR 0009's "what is not claimed" section is revised to say what the new mechanism does claim, and
  the schema description for `packSelfMaintenance` with it. Neither may claim more than the
  implementation supports — the failure that opened this line of work. In particular neither may
  claim origin for an operator who took the anchor from the pack.
- Every origin-dependent assertion in this codebase goes through one contract that refuses unless
  `origin.status === ESTABLISHED`, and no such assertion reads `status` or `workingTreeStatus`
  instead. The point of a single contract is that a future consumer cannot make the claim by
  accident; appending the field and trusting callers to read it is what ADR 0009's property 2 already
  cost once.
- The mechanism (GPG, SSH signing, minisign, other) is chosen after the falsifier exists, and the
  choice is justified against it.

## Where the requirement binds — decided

The owner decided on 2026-08-16: **`maintain` reports origin; it does not require it. Anything that
claims canonical origin must require it.** The full statement and the reason are in ADR 0010.

Eligibility for `maintain` therefore stays lineage-based, because `maintain` is not an
origin-authority operation — it answers whether this working tree satisfies the standards it
publishes, and making that answer depend on release-owner credentials would put ordinary development
behind trust configuration contributors correctly should not hold.

The envelope carries origin as a named state with a reason, never a boolean:

```
status: SELF_MAINTENANCE
workingTreeStatus: COMPLIANT

origin:
  status: ESTABLISHED | NOT_ESTABLISHED
  reason: trust-anchor-absent | untrusted-signer | invalid-signature | release-unavailable
  trustedKey: ...
```

`originVerified: false` is rejected: a boolean lets a caller collapse "checked and rejected", "could
not check", and "no anchor was supplied" into one branch, and those three are the distinction the
mechanism exists to make.

The "field nobody reads" risk is answered structurally rather than by hoping. Any API making an
origin-dependent assertion goes through a contract that requires `ESTABLISHED`; reading `status` or
`workingTreeStatus` alone can never produce a positive canonical-origin result, and a test says so.

## The three acceptance tests, written before the plumbing is chosen

1. **No anchor.** Legitimate pack lineage, no externally supplied anchor → maintenance can succeed,
   origin is `NOT_ESTABLISHED / trust-anchor-absent`.
2. **Attacker fork.** Full fork carrying an attacker-controlled in-repo key and a correct signature
   over it, evaluated against an independently trusted real fingerprint → maintenance may run, origin
   remains `NOT_ESTABLISHED`.
3. **Origin-dependent consumer.** Feed either of those outputs into the canonical-origin assertion
   path → it must refuse.

Together with the substitution falsifier above these preserve both invariants: development remains
achievable without release authority, and origin authority remains impossible without externally
supplied trust.

## Landed so far — the contract, not the cryptography

`scripts/pack-origin.mjs` holds the origin states, the five reasons, and `assertCanonicalOrigin` —
the one door every origin-dependent assertion goes through. The verifier is injected, so the module
is complete without a mechanism and the mechanism can be chosen against the falsifier rather than
around an existing implementation.

Two properties are worth naming because neither is visible in a passing happy path:

- **The anchor is checked before anything else**, which makes "no anchor supplied" unreachable from
  every later branch. No verifier — including a hostile one a fork supplies — can produce
  `ESTABLISHED` for an operator who never configured trust.
- **The module imports no filesystem access at all**, and a test asserts the absence structurally. A
  future edit adding "fall back to `scripts/trusted-key.json` when no anchor is supplied" is the
  rejected design, and it would pass every behavioural test here because it only makes the happy path
  easier. A module that cannot open a file cannot be talked into it.

The tests were written after the module rather than before it, which is recorded in the test header
rather than glossed: they did not fail first against an absent implementation, and four mutations
against the committed module are the compensating evidence. Two mutation results contradicted the
prediction and both are recorded as observed — including one showing a test passing for a reason
other than the one it was written for.

## The mechanism: SSH signatures, and the falsifier met

`scripts/ssh-tag-verifier.mjs` reads the signature out of the annotated tag and reports **who signed**,
leaving the trust comparison in `packOrigin` where it already lived. Chosen against the falsifier
rather than for ergonomics:

1. **The anchor is a value, not ambient state.** Verification takes the operator's key as an argument
   and writes the allowed-signers file into a directory it creates. GPG's answer comes from the
   invoking user's keyring and ownertrust database — external to the pack, so it satisfies the letter
   of ADR 0010, but no caller passes it in and its own notion of trust is a second model layered
   under ours.
2. **It signs the release object**, not a detached file that would need its own binding back to it.
3. **No new dependency.** `ci/Dockerfile` has no `RUN` instruction by design, so anything used must
   already be in `node:20-bookworm`. `ssh-keygen` 9.2 and `git` 2.39 both are. A mechanism needing an
   install would have had to change that file's central claim.

**`git verify-tag` is not used, and that is the security-relevant part.** Git resolves the
allowed-signers file through `gpg.ssh.allowedSignersFile` — configuration the *evaluated repository*
controls. Verifying through Git would let the pack under evaluation nominate the file deciding
whether to believe it: the in-pack anchor arriving through a side door rather than the front one ADR
0010 closed. A test holds that line with a fork whose own configuration satisfies `git verify-tag`
and which is refused anyway.

The owner's falsifier is met with real keys and real signatures — no stub verifier, since a fake
verifier can be made to say anything and that is precisely what is under test. The fork generates its
own ed25519 key, signs a genuine release tag, and ships its own public key as in-repo trust
configuration. **The test asserts positively that the fork's release verifies under the fork's own
anchor**, so it cannot pass by the fork being incompetent; the only difference between the accepted
and refused calls is where the trust came from.

Three mutations, each killed with a distinct signature, recorded in the test header. The one worth
naming: a verifier reporting the *anchor's* fingerprint instead of the *signer's* would make every
signature appear to be the custodian's, and it reddens the falsifier and the config test together.

## Complete — 2026-08-16, for the HFN portion only

The checklist below is the definition, and what it closes is narrower than this item's title
suggests. **Self-maintenance eligibility does not now rest on a signature**, because no check running
inside the pack can make it do so; what rests on a signature is *canonical origin*, established by a
trusted host from outside. The title is left as it was written so the record shows what was expected
of this work and what it turned out to be.

Nothing here delivers full-fork exclusivity, and no surface in this repository may say it does.

## Two defects in the reference mechanism, found in review before merge

Both were in this repository's protocol and reference layer rather than in anything deferred to the
host, and both were specification gaps as much as code defects — the contract has been amended with
each, since a host implementing it correctly would otherwise have inherited them.

**Signed-tag relabelling.** `readSignedTag` trusted the caller's ref name and never read the signed
tag object's own `tag <name>` header. A fork points `refs/tags/v9.9.9` at the custodian's genuine
signed `v1.1.0` tag object, and every check passes: the commit matches, the tree matches, the signer
is the custodian, the signature verifies — because all of it really is the custodian's work. The only
false thing is which release the evidence is offered for, and **comparing oids cannot catch it,
because the oids are identical by construction.** The name binding is what the signature has to
authorise. It is reported as `release-name-mismatch`, a contradicted state of its own: somebody built
that ref, and the operator action is to find out who rather than to look at the key or the tooling.

**Unavailable verification reported as invalid.** A missing, unspawnable, or too-old `ssh-keygen`
produced a non-zero status, which read identically to a refused signature and became
`invalid-signature`. That asserts the signature was examined and found wanting when nothing examined
it — a claim nobody performed, and a false accusation against whoever signed. The verifier now
answers in three states, and `verification-unavailable` is kept apart from
`verification-unimplemented` because the operator actions differ: repair the tool, versus this was
never built.

The falsifiers are paired on purpose. Mapping every cryptographic failure to `unavailable` would
satisfy the missing-tool test on its own and is the easier wrong fix, so the tampered-payload control
sits beside it and forbids that.

## The finding that reshaped this item: the anchor was external, the judge was not

Review of the SSH slice found the assumption underneath the whole mechanism. `pack-origin.mjs` and
`ssh-tag-verifier.mjs` live inside the pack whose origin they authenticate. An honest fork running
them is refused; a hostile fork rewrites either file and reports `ESTABLISHED` without going near
`ssh-keygen`. **Supplying the real public key does not prevent that** — the key is external and the
code interpreting the evidence is not.

So the rule this item is now built on, recorded in
[ADR 0011](../../adr/0011-canonical-origin-cannot-be-asserted-by-the-pack.md):

> **Canonical-origin establishment requires a verifier whose implementation is outside the evaluated
> pack's control.** The evaluated pack may provide the signed tag, the standards bytes, a reference
> implementation, and diagnostics. It may not pronounce on its own canonical origin.

`test/trusted-execution-boundary.test.mjs` makes it observable: the same tag, the same anchor, the
same question answered twice — once by the module loaded from the evaluated pack, once by the module
the host already had — and the answers disagree. Which one an operator gets is decided entirely by
where the code came from.

**The anchor is supplied as a key value, not a path**, to the trusted verifier — the verifier derives
the fingerprint, and configuring both representations is avoided unless rotation needs it. A path is
acceptable only at the trusted-host boundary, where the host reads it before invoking anything from
the pack. "Read this path for your anchor" is never passed to evaluated code.

## Completion, split mechanically

Three categories, not two, and the third is what stops either side from lying. ST-12 can complete
without inventing a `v1.1.0`, and this repository still cannot claim the signing regime has been
exercised until a real release exists.

```
ST-12 / HFN
COMPLETE when:
✔ signing format frozen                    SSH (ssh-ed25519); scripts/ssh-tag-verifier.mjs
✔ custodian / trust-anchor governance      ADR 0010
✔ host-verifier contract frozen            docs/design/external-verifier-contract.md
✔ adversarial + positive vectors exist     test/host-verifier-conformance.test.mjs
✔ reference SSH mechanism demonstrates     scripts/ssh-tag-verifier.mjs + its real-key tests
✔ human release-signing procedure frozen   docs/release-signing.md

Portfolio guarantee
NOT COMPLETE until StandardsEnforcer:
□ implements the host contract
□ receives the trusted key outside the pack
□ passes the hostile-fork falsifier from that boundary

Release-bound production evidence:
□ first new release under this regime is actually signed and externally verified
```

**Freezing the procedure and executing it are separate**, which is why the last line is its own
category rather than a blocker on the first. Requiring a release in order to close this item would
manufacture a release to satisfy process, and `v1.0.0` cannot acquire signing retroactively anyway
([ADR 0008](../../adr/0008-authenticity-guarantees-are-not-retroactive.md)). The first new signed
release is the production proof of the procedure, not a prerequisite for having one — the same
non-retroactivity rule this line of work has now met four times.

## What this item can close, and what it cannot

- **Here:** the signing format, the release-signing procedure, signed-tag verification semantics, the
  states and reasons, and the test vectors and adversarial forks. ST-12 completes when the
  cryptographic protocol, the external-verifier contract, and the human signing procedure are frozen
  and testable — the checklist above is the single definition, and no other surface in this item
  states a different one.
- **Not here:** full-fork exclusivity. The trusted execution boundary necessarily lives outside this
  repository, so the portfolio-level origin guarantee completes only when StandardsEnforcer
  implements the contract from outside the evaluated pack. Nothing in this repository may report that
  guarantee as delivered.

### Still to do

- Anchor ingestion and authoritative verification move to StandardsEnforcer. The anchor-supply
  plumbing is no longer a question for this pack — it is a question for the trusted host, which is
  the point.
- ~~Wire `origin` into the `maintain` envelope.~~ **Not happening in this repository, decided by the
  owner on 2026-08-16.** It was first held back because the field could only say
  `verification-unimplemented`; the boundary changes the conclusion rather than the schedule. The
  authoritative origin disposition belongs to the **host envelope around** the pack result, not
  inside it — a field inside the pack's own output is the pack asserting its own origin, in the place
  a consumer is most likely to read it. If a host later passes a non-authoritative echo in for
  display, it must be explicitly labelled host-supplied evidence and never presented as something the
  pack established. A test asserts the CLI imports no origin machinery.
- Revise ADR 0009's "what is not claimed" and the `packSelfMaintenance` schema description once the
  mechanism exists, per the acceptance criteria above.

## Why it was not done sooner

Two of the three original reasons are discharged. The key-management commitment is decided (ADR
0010). The temptation to ship a `git verify-tag` call that passes when no key is configured is now
forbidden explicitly rather than resisted informally.

The third stands and is not a blocker: **the certified releases predate it.** `v1.0.0` is unsigned and
cannot be signed now without re-tagging, which is the moving-reference-wearing-an-immutable-name
problem FE-13 detects ([ADR 0008](../../adr/0008-authenticity-guarantees-are-not-retroactive.md)).
Signing it retroactively would not help anyway: a fork copies a genuine signed historical tag as
easily as an unsigned one. The property becomes strongest at the first *new* release signed under
this regime, from which point a fork can copy already-authorized releases but cannot mint a new
authoritative one. Verification therefore establishes a floor at the first signed release, exactly as
FE-13's own mechanism does.
