---
id: FE-05
type: feature
title: Test suite, documentation, and the CI gate
parent: EP-01
status: COMPLETE
opened: 2026-08-09
closed: 2026-08-09
evidence:
  - 98b9b91
  - cdda7d6
  - a1150b2
tags: [build, tests, ci]
---

## What

The `node:test` suite with a mutation test per guard, the README and INSTRUCTIONS, the
architecture reference and its canonical Mermaid source, and a CI workflow with no install step.

## Why

A guard nobody has watched fail is a guard nobody knows works. Every guard here has a test that
reintroduces the exact defect it exists to catch and asserts the guard fails.

## Notes

CI accepts exit 4 visibly, with the reason written into the workflow rather than hidden in a shell
expression — the repository's own verdict is honestly `NOT_EVALUATED` until human attestations
exist, and mapping that to 0 would be the exact false green the whole system is built to avoid.
Removing that allowance is part of ST-06.
